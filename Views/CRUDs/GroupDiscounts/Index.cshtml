@model IEnumerable<event_booking.Models.GroupDiscount>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GroupName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MinimumAdults)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MinimumChildren)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PriceMultiplier)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GroupName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinimumAdults)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MinimumChildren)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.PriceMultiplier)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GroupDiscountId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GroupDiscountId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GroupDiscountId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
